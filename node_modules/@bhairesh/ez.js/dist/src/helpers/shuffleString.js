"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const isStr_1 = __importDefault(require("../helpers/isStr"));
/**
 * Shuffles the characters of a given string and returns the shuffled string.
 *
 * @param {string} str - The input string to be shuffled.
 * @returns {string} The shuffled string.
 */
function shuffleString(str) {
    str = str.trim().replace(/ /g, "");
    if (!(0, isStr_1.default)(str))
        return str;
    const array = str.split("");
    // Fisher-Yates Shuffle Algorithm
    for (let i = array.length - 1; i > 0; i--) {
        const j = Math.floor(Math.random() * (i + 1));
        [array[i], array[j]] = [array[j], array[i]];
    }
    return array.join("");
}
exports.default = shuffleString;
